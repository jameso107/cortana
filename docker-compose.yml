version: "3.9"

services:
  searxng:
    image: searxng/searxng:latest
    container_name: cortana-searxng
    ports:
      - "8888:8080"
    volumes:
      - ./config/searxng:/etc/searxng:rw
    environment:
      - SEARXNG_BASE_URL=http://localhost:8888/
    restart: unless-stopped
